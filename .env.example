# Optional: your live REVNCIA AI Gateway / receptionist API
# POST {text, gesture} should return mouth + hand keyframes (see lib/robotMotion.ts).
# If unset, the site uses the built-in REVNCIA motion engine.

REVNCIA_ROBOT_API_URL=
REVNCIA_ROBOT_API_KEY=
